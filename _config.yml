# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Cakes.dk
email: morten@cakes.dk
description: >- # this means to ignore newlines until "baseurl:"
  Eventually this will become the cake index of the internet
baseurl: "" # Leave empty if using custom domain
url: "https://www.cakes.dk"
github_username: mortenduus

# Navigation pages (for minima theme)
header_pages:
  - about.markdown
  - recipes/index.html
  - tags/index.html

# Collections
collections:
  recipes:
    output: true
    permalink: /recipes/:slug/

defaults:
  - scope:
      path: ""
      type: "recipes"
    values:
      layout: recipe
      share: true
      image: /assets/images/placeholder.svg
      lang: da

# Build settings
theme: minima
# GitHub Pages specific
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag

# Sass / CSS pipeline (optional future enhancement)
sass:
  style: compressed

# Custom site variables
site_theme:
  color_primary: "#D94E62" # raspberry
  color_secondary: "#F7C948" # custard
  color_bg: "#FFF9F7"
  color_text: "#2E2A27"
  radius: "10px"
  font_heading: "'Poppins', 'Helvetica Neue', Arial, sans-serif"
  font_body: "'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen"


# Build settings
markdown: kramdown
highlighter: rouge
# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
